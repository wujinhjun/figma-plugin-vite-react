{
  "name": "figma-vite",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "lerna run --stream --scope figma-vite-ui --scope figma-vite-core dev"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
